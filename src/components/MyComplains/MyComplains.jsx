import React, { useState } from 'react';

const MyComplains = () => {

    //this will be the complain dashboard segment for renters

    const [complains, setComplains] = useState();

    return (
        <div className='m-4'>
            complains
        </div>
    );
};

export default MyComplains;